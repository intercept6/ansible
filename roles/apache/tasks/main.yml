# Install Apache

- name: Install apache
  yum: name=httpd state=latest

- name: Apache update the httpd.conf
  template:
    src: httpd.conf.j2
    dest: /etc/httpd/conf/httpd.conf
    owner: root
    group: root
    mode: 0644
  notify:
    - restart httpd
